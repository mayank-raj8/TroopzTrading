<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trading">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_trading_craft"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trading">
        <activity android:name=".Profile.EditProfile"></activity>
        <activity android:name=".CommentsActivity" />
        <activity android:name=".RetreivePostActivity" />
        <activity android:name=".CreatePostActivity" />
        <activity android:name=".TradersChatActivity" />
        <activity android:name=".GroupsFragment" />
        <activity android:name=".ExampleActivity" />
        <activity android:name=".GroupchatActivity" />
        <activity android:name=".CourseActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".Authentication.LogInScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Authentication.SignUpScreen" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>